from setuptools import setup, find_packages

setup(
    name='utils',  # Name of your package
    version='0.1',
    packages=find_packages(),
    install_requires=[
    ],
    description='Utils custom package'
)
